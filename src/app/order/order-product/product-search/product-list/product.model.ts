export class ProductModel {

  constructor(public id: number, public code: string, public name: string, public desc: string, public imgPath: string, public qty: number, public mrp: number, public dp: number, public bv: number, public pv: number) {
  }

  // constructor(public id: number, public name: string) { }

}
